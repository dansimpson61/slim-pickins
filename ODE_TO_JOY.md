# Ode to Joy — moved

This was one of nine byte-identical copies of the long-form Ode scattered
across `~/dev`. Nine copies meant nine things that could drift, and no way to
tell which one was right.

**The canonical text is `~/dev/ode-to-joy/ODE_TO_JOY.md`.** Read that.

It supersedes this copy: same convictions, better organised, and it adds the
precedence order (correctness > the house > clarity > idiom > elegance), the
operating procedure, and the view-layer rules this text only gestured at.

The full historical essay is preserved at
`~/dev/archive/ode-to-joy-drafts/ODE_TO_JOY-longform.md`.

*Reconciled 2026-08-28.*
